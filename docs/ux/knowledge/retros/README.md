# Retrospectives

Workers add `{story}-{role}.md` here after an assignment or L6.

Template:

```markdown
# Retro — G-0X — {role}

- Surprised:
- Hard:
- Reuse:
- Avoid:
```

Librarian indexes into patterns/decisions.
